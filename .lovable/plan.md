

## Delete Test Lead

Delete the test record `ghltest@best365labs.com` from the `leads` table using a SQL DELETE statement via the data operation tool.

**SQL:**
```sql
DELETE FROM public.leads WHERE email = 'ghltest@best365labs.com';
```

Single operation, no schema or code changes required.

